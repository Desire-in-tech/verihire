# Copy this file to .env and fill in your key.
# Without a key set, this service still runs, using an offline
# keyword-based fallback extractor instead of calling Claude.

ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-opus-4-5
AI_SERVICE_HOST=0.0.0.0
AI_SERVICE_PORT=8001
