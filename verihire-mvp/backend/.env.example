# Copy this file to .env

AI_SERVICE_URL=http://localhost:8001
BACKEND_HOST=0.0.0.0
BACKEND_PORT=8000

# Leave this blank to use the offline mock proof (midnight_mock.py).
# Set it once midnight_service/ is actually running (see that folder's
# README for the real Midnight toolchain setup) to switch on real proofs.
MIDNIGHT_SERVICE_URL=
